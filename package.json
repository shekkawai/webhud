{
  "name": "webhud",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "test": "bun test",
    "deploy": "bun run build && wrangler deploy"
  },
  "devDependencies": {
    "@types/leaflet": "^1.9.22",
    "typescript": "^5.6.0",
    "vite": "^6.0.0",
    "wrangler": "^4.127.1"
  },
  "dependencies": {
    "leaflet": "^1.9.4"
  }
}
